name: Manual Wrkfl

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/intro-manual-workflownp.yml'
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Hows GitHub Actions?
        run: echo "Awesome!!"
